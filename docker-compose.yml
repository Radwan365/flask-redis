services:
  web:
    build: .
    ports:
      - "5000"
    env_file:
      - .env
    depends_on:
      - redis
  redis:
    image: redis
    ports:
      - "6379:6379"
    volumes:
    - redis-data:/data

volumes:
  redis-data: